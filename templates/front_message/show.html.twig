{% extends 'base.html.twig' %}

{% block title %}FrontMessage{% endblock %}

{% block body %}
    <h1>FrontMessage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ front_message.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ front_message.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ front_message.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('front_message_index') }}">back to list</a>

    <a href="{{ path('front_message_edit', {'id': front_message.id}) }}">edit</a>

    {{ include('front_message/_delete_form.html.twig') }}
{% endblock %}
