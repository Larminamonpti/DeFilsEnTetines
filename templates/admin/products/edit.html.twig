{% extends 'admin.layout.html.twig' %}

{% block title %}Edit Products{% endblock %}

{% block main %}

    <div class="formulaire">

        <h1 class="text-center text-bleu">Modifier</h1>

        {{ include('admin/products/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="d-flex justify-content-center">
            <a class="btn btn-rose" href="{{ path('products_index') }}">Retour à la liste</a>
        </div>

        
</div>
{% endblock %}
{% block javascripts %}
    <script src="{{asset('js/embed.js')}}"></script>
{% endblock %}
