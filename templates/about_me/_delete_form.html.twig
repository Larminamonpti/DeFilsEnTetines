<form method="post" action="{{ path('about_me_delete', {'id': about_me.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ about_me.id) }}">
    <button class="btn">Delete</button>
</form>
