{% extends 'layout.html.twig' %}

{% block title %}Modifier l'adresse{% endblock %}

{% block main %}
    <div class="formulaire">

        <h1 class="text-center">Modifier l'adresse</h1>
        
        {{ include('adress/_form.html.twig', {'button_label': 'Modifier'}) }}
        
        
        
    </div>
    <div class="d-flex justify-content-center">
        <a class="btn btn-bleu mb-2" href="{{ path('user_edit', {'id' : app.user.id}) }}">Retour</a>
    </div>
    
{% endblock %}
