{% extends 'layout.html.twig' %}

{% block title %}New Adress{% endblock %}

{% block main %}
    <div class="formulaire">

        <h1 class="text-center">Ajouter un adresse</h1>
        
        {{ include('adress/_form.html.twig', {'button_label': 'Ajouter'}) }}
        
    </div>
        <div class="d-flex justify-content-center">
            <a class="btn btn-bleu" href="{{ path('user_edit', {'id' : app.user.id}) }}">Retour</a>
        </div>
{% endblock %}
